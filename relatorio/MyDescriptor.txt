# COMANDOS DEV

	./mvnw quarkus:add-extension -Dextensions="hibernate-orm-panache,jdbc-postgresql,resteasy-jackson"

# DOCKER

	./mvnw clean package -DskipTests
	docker build -f src/main/docker/Dockerfile.jvm -t quarkus/relatorio-jvm .
	docker run -i --rm -p 8080:8080 quarkus/relatorio-jvm

# pom.xml

	<dependency>
	  <groupId>org.apache.commons</groupId>
	  <artifactId>commons-csv</artifactId>
	  <version>1.10.0</version>
	</dependency>